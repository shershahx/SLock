<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- App Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:gravity="center"
            android:text="@string/app_name"
            android:textColor="#212121"
            android:textSize="28sp"
            android:textStyle="bold" />

        <!-- Service Status Indicator -->
        <TextView
            android:id="@+id/tvServiceStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:background="@drawable/bg_status_card"
            android:gravity="center"
            android:padding="14dp"
            android:text="@string/status_service_inactive"
            android:textColor="#D32F2F"
            android:textSize="15sp"
            android:textStyle="bold" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginBottom="20dp"
            android:background="#E0E0E0" />

        <!-- Accessibility Instructions -->
        <TextView
            android:id="@+id/tvInstructionsAccessibility"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:lineSpacingExtra="4dp"
            android:text="@string/instructions_accessibility"
            android:textColor="#424242"
            android:textSize="14sp" />

        <!-- Button: Open Accessibility Settings -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAccessibilitySettings"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginBottom="28dp"
            android:text="@string/btn_accessibility_settings"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:letterSpacing="0.02"
            app:backgroundTint="#1976D2"
            app:cornerRadius="30dp"
            app:elevation="6dp"
            app:icon="@android:drawable/ic_menu_preferences"
            app:iconTint="#FFFFFF"
            app:iconGravity="textStart"
            app:iconPadding="12dp"
            app:iconSize="24dp" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginBottom="20dp"
            android:background="#E0E0E0" />

        <!-- Battery Optimization Instructions -->
        <TextView
            android:id="@+id/tvInstructionsBattery"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:lineSpacingExtra="4dp"
            android:text="@string/instructions_battery"
            android:textColor="#424242"
            android:textSize="14sp" />

        <!-- Button: Open Battery Optimization Settings -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBatterySettings"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginBottom="16dp"
            android:text="@string/btn_battery_settings"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:letterSpacing="0.02"
            app:backgroundTint="#F57C00"
            app:cornerRadius="30dp"
            app:elevation="6dp"
            app:icon="@android:drawable/ic_lock_idle_low_battery"
            app:iconTint="#FFFFFF"
            app:iconGravity="textStart"
            app:iconPadding="12dp"
            app:iconSize="24dp" />

    </LinearLayout>

</ScrollView>
